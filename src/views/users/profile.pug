extends ../base
include ../mixins/video

block content
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.username
    div.video-grid
        each item in user.videos 
            +video(item)
        else 
            span.empty-message No Videos Found