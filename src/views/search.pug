extends base
include mixins/video

block content
    form(method="get").search__form
        input(type="text", name="keyword", placeholder="Search By Title", required)
        button 
            i.fas.fa-search
    
    div.video-grid
        each video in videos
            +video(video)
        else 
            span.empty_message No vidoes found        